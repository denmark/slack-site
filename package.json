{
  "name": "slack-site",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i ./static/input.css -o ./static/app.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
